{
    "name": "ts-workerpool",
    "version": "0.1.0",
    "scripts": {
        "start": "tsc --build tsconfig.json & ts-node src/app.ts",
        "build": "tsc --build tsconfig.json",
        "trace": "tsc --traceResolution"
    },
    "devDependencies": {
        "@types/node": "^22.13.9",
        "ts-loader": "^9.5.2",
        "ts-node": "^10.0.0",
        "typescript": "^4.5.0"
    }
}
